# PR 11 - Spec Decode And EAGLE Acceptance Behavior

## Reviewer Summary

This PR should only adjust spec-decode expectations where the backend or
architecture justifies it. The MI325 GPT-OSS acceptance regression was small
but real, so the PR must show before/after acceptance numbers rather than only
relaxing a tolerance.

## Exact AMD Labels

- `V1 Spec Decode`
- `Spec Decode Eagle`
- `Spec Decode Speculators + MTP`
- `V1 e2e (4 GPUs) - Part 1`
- `V1 e2e (4 GPUs) - Part 2`
- `V1 e2e (4 GPUs) - Part 3`
- `V1 e2e (4 GPUs) - Part 4`
- `Acceptance Length Test (Large Models)`

## Files

- `tests/v1/spec_decode/test_acceptance_length.py`
- `tests/v1/spec_decode/test_eagle.py`
- `tests/v1/e2e/general/test_cascade_attention.py`

## Failure Basis

- `mi325_1: V1 Spec Decode` failed
  `test_eagle3_acceptance_length[TRITON_ATTN-tp1-3-gpt-oss-20b-eagle3]`
  with a per-position acceptance-length drop just above tolerance.
- Earlier notes suggested avoiding ROCm attention backends that do not support
  sinks and using a backend with the required feature support.
- `V1 e2e (4 GPUs)` had long-running Llama 4 EAGLE multimodal cases; sharding
  belongs in PR 01, while backend/correctness expectations belong here.

## Reviewer Questions We Must Answer

- Is the backend switch based on feature support, not just making the test pass?
- What are the expected and actual acceptance lengths before and after?
- Is the failure MI325-only, MI300-only, MI355-only, or all ROCm?
- Are multimodal EAGLE timing problems separate from acceptance correctness?

## Validation Commands

- Run the exact failing MI325 acceptance case.
- Run the MI300 EAGLE heavy cases split across the four V1 e2e parts.
- Record per-position acceptance lengths in the PR body.

## Weak Confidence

- Tolerance-only changes are weak.
- Backend changes without a feature-support explanation are weak.
