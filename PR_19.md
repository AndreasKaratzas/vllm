# PR 19 - Review Artifacts And Follow-Up Issues

## Reviewer Summary

This is not a product PR. It is a tracking bundle for review notes, proposed PR
descriptions, and follow-up issue drafts. These files should not merge into the
main repository unless maintainers explicitly want this documentation checked
in.

## Files

- `PR_01.md`
- `PR_03.md` through `PR_20.md`
- `ROCM_CI_DECISIVE_BLOG.md`
- `issue_1.md`
- `issue_2.md`
- `issue_3.md`
- `rocm_ci_progress.md`

`PR_02.md` was retired after the Docker/non-root image cleanup files were
removed from the proto diff.

## Exact AMD Labels

None. This PR is documentation and issue-tracking only.

## What The Review Artifacts Must Do

- Map every changed file to a proposed PR or explicitly mark it weak.
- Use exact labels from `.buildkite/test-amd.yaml`, not approximate names.
- Call out changes that should probably be dropped from the ROCm CI stack.
- List validation commands and missing benchmark evidence.

## Follow-Up Issues To File

- Llama 4 EAGLE multimodal rendering/compile hotspot in `V1 e2e (4 GPUs)`.
- MI355-specific AITER inaccuracy, kept separate from MI300/MI325 fixes.
- NCCL/HIP invalid-argument failures that pass in nightly but fail on this
  branch, requiring Buildkite environment A/B analysis.

## Validation

- `git status --short --untracked-files=all` count matches the blog.
- `git diff --name-only` count matches the blog.
- Every PR note uses exact label names or explicitly says no exact label exists.

## Weak Confidence

- These notes are useful for review but should eventually become PR bodies,
  Buildkite issue comments, or external tracking docs.
