# PR 17 - Entrypoints, API Protocol, Tool Parser, Pooling, And Speech

## Reviewer Summary

This PR should be a set of entrypoint-facing fixes with architecture-specific
accuracy handling. It must distinguish public API behavior changes from test
expectation changes.

## Exact AMD Labels

- `Entrypoints Unit Tests`
- `Entrypoints Integration (API Server 2)`
- `Entrypoints Integration (Speech to Text)`
- `Entrypoints Integration (Pooling)`
- `Entrypoints Integration (Responses API)`
- `OpenAI API correctness`

## Files

- `tests/entrypoints/openai/tool_parsers/test_granite4_tool_parser.py`
- `tests/entrypoints/pooling/embed/test_online.py`
- `tests/entrypoints/pooling/scoring/test_cross_encoder_online_vision.py`
- `tests/entrypoints/speech_to_text/correctness/test_transcription_api_correctness.py`
- `tests/tool_parsers/test_mistral_tool_parser.py`
- `vllm/entrypoints/anthropic/api_router.py`
- `vllm/entrypoints/openai/engine/protocol.py`

## Failure Basis

- Speech-to-text WER failed on MI300 with a measured value different from the
  expected baseline.
- Weight-transfer unit tests failed in `Entrypoints Unit Tests`.
- Pooling and tool parser tests touched public request/response behavior.

## Reviewer Questions We Must Answer

- Are WER baselines architecture-specific using ROCm arch helpers, not broad
  ROCm gates?
- Do protocol changes alter public API schema or only test helpers?
- Are tool parser changes aligning with model output, or weakening parser
  validation?
- Are pooling test changes caused by backend numerics or by API behavior?

## Validation Commands

- Run `Entrypoints Unit Tests`.
- Run `Entrypoints Integration (Speech to Text)` and record WER values by
  architecture.
- Run `Entrypoints Integration (Pooling)`.
- Run focused tool parser tests for Granite and Mistral.

## Weak Confidence

- WER threshold changes without per-architecture data are weak.
- Public protocol changes need API compatibility notes.
