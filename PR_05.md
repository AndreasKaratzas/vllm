# PR 05 - Compile, Fullgraph, And Fusion Pass Gating

## Reviewer Summary

This PR is lower priority for the ROCm CI recovery because PyTorch compile
groups were explicitly deprioritized. Keep it separate from runtime fixes. If a
fusion-pass change is needed by a non-compile MI300 label, the PR must name
that label and show the failing graph pattern.

## Exact AMD Labels

- `PyTorch Compilation Unit Tests`
- `PyTorch Fullgraph`
- `PyTorch Fullgraph Smoke Test`
- `PyTorch Compilation Passes Unit Tests`
- `Fusion E2E Config Sweep (H100-MI300)`
- `Fusion E2E Quick (H100-MI300)`

## Files

- `tests/compile/fullgraph/test_basic_correctness.py`
- `tests/compile/fullgraph/test_full_graph.py`
- `tests/compile/passes/distributed/test_async_tp.py`
- `tests/compile/test_dynamic_shapes_compilation.py`
- `vllm/compilation/passes/fusion/collective_fusion.py`
- `vllm/compilation/passes/fusion/matcher_utils.py`
- `vllm/compilation/passes/fusion/rms_quant_fusion.py`
- `vllm/compilation/passes/fusion/rope_kvcache_fusion.py`

## What Reviewers Need To See

- The unsupported ROCm graph/fusion pattern before the patch.
- Whether the change is a runtime compiler fix or only a test skip.
- Why any skip is keyed on backend/architecture rather than broad ROCm.
- One representative compiled graph before/after if a fusion pass changes.

## Validation Commands

- Collect-only for all touched compile tests.
- One MI300 run of `PyTorch Fullgraph Smoke Test`.
- One MI300 run of `Fusion E2E Quick (H100-MI300)` if runtime fusion code
  changes remain in the PR.

## Weak Confidence

- Broad ROCm compile skips are weak. They should become feature gates whenever
  possible.
- Fusion pass edits need correctness evidence; collect-only is not enough.
