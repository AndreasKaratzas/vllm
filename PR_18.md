# PR 18 - MHC TileLang And Mixed-Precision GEMM Paths

## Reviewer Summary

This PR is for low-level kernel path changes that are not covered by PR 07 or
PR 08. It needs compile-time and correctness evidence. The known open issue is
that TileLang/Triton compilation can become a large hotspot, so benchmark data
should include compile time as well as runtime.

## Exact AMD Labels

- `Qwen3-Next-80B-A3B-Instruct MTP Async EPLB Accuracy`
- `Hybrid SSM NixlConnector PD accuracy tests (4 GPUs)`
- `Kernels Quantization Test %N`
- `Kernels Core Operation Test`

## Files

- `vllm/model_executor/kernels/linear/mixed_precision/triton_w4a16.py`
- `vllm/model_executor/kernels/mhc/tilelang.py`
- `tests/kernels/quantization/test_rocm_skinny_gemms.py`

## Reviewer Questions We Must Answer

- Which failing model path uses MHC TileLang?
- Is the problem compile latency, numerical correctness, or a launch failure?
- Does the mixed-precision GEMM change affect CUDA, ROCm, or both?
- What are the before/after compile and execution times?

## Validation Commands

- `pytest -v -s tests/kernels/quantization/test_rocm_skinny_gemms.py`
- Run the exact Qwen3-Next final Buildkite command if TileLang is implicated.
- Capture first-run compile time and second-run cached runtime.

## Weak Confidence

- Kernel changes without benchmark tables are weak.
- If the only issue is compile latency, the PR should file an issue and avoid
  risky runtime changes unless they are clearly needed.
