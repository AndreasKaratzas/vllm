# PR 13 - Simple KV Offload And Filesystem Tiering I/O

## Reviewer Summary

This PR is related to V1 KV/offload failures, but it should stay separate from
the NIXL/Mooncake connector PR unless the same failing log proves both are
required. Filesystem tiering changes need careful review around partial writes,
flush/close, and cancellation.

## Exact AMD Labels

- `V1 Core + KV + Metrics`
- `V1 others (CPU)`

## Files

- `tests/v1/simple_kv_offload/test_scheduler.py`
- `vllm/v1/simple_kv_offload/manager.py`
- `vllm/v1/kv_offload/tiering/fs/io.py`

## Why These Changes Exist

The changed files suggest two related but distinct goals:

- scheduler correctness for simple KV offload;
- safer filesystem tiering I/O.

The PR needs to say whether the Buildkite failure was an assertion failure,
hang, data corruption, cleanup leak, or timeout. Without that, reviewers cannot
tell whether the I/O change is necessary.

## Reviewer Questions We Must Answer

- What exact failure in `V1 Core + KV + Metrics` does this fix?
- Does the filesystem I/O code handle short writes and interrupted flushes?
- Are test expectations changed because behavior was wrong, or because timing
  changed?

## Validation Commands

- `pytest -v -s tests/v1/simple_kv_offload/test_scheduler.py`
- Focused `V1 Core + KV + Metrics` command from `.buildkite/test-amd.yaml`.
- I/O fault-injection or at least a partial-write-oriented unit test if the
  filesystem layer remains changed.

## Weak Confidence

- The filesystem I/O changes are weak unless paired with a concrete failing
  stack trace or data-integrity test.
